# lefthook.yml -- Pre-commit and pre-push hooks.
#
# Language-specific blocks are activated by ./setup.sh.
# To configure manually: uncomment the block for your language,
# delete the placeholder command, and run: lefthook install

pre-commit:
  parallel: true
  commands:

    # [typescript]
    # lint:
    #   glob: "src/**/*.{ts,tsx}"
    #   run: npx eslint {staged_files} --no-error-on-unmatched-pattern
    # type-check:
    #   glob: "src/**/*.{ts,tsx}"
    #   run: npx tsc --noEmit
    # [/typescript]

    # [go]
    # vet:
    #   glob: "**/*.go"
    #   run: go vet ./...
    # fmt:
    #   glob: "**/*.go"
    #   run: test -z "$(gofmt -l {staged_files})"
    # [/go]

    # [python]
    # lint:
    #   glob: "**/*.py"
    #   run: ruff check {staged_files}
    # format:
    #   glob: "**/*.py"
    #   run: ruff format --check {staged_files}
    # [/python]

    # [swift]
    # swiftlint:
    #   glob: "**/*.swift"
    #   run: swiftlint lint {staged_files}
    # [/swift]

    # [kotlin]
    # ktlint:
    #   glob: "**/*.kt"
    #   run: ktlint {staged_files}
    # [/kotlin]

    placeholder:
      run: "echo 'Run ./setup.sh to configure hooks for your language'"

pre-push:
  commands:
    push-guard:
      run: |
        if [ -f agent.json ]; then
          ALLOW=$(jq -r '.git.allow_push // "false"' agent.json)
          if [ "$ALLOW" = "false" ]; then
            echo "Push blocked by agent.json (allow_push: false)"
            echo "Operator override: git push --no-verify"
            exit 1
          fi
        fi
